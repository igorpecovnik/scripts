#
# This action recreate action for building stable images
#
name: ABC
on:
  workflow_dispatch:
  repository_dispatch:
    types: [Recreate Matrix]

env:
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:


  build:
    name: Recreate action
    runs-on: ubuntu-latest
    steps:

    - name: Test
      run: |
        echo "ABC"
        gh api -H "Accept: application/vnd.github+json" \
        -H "X-GitHub-Api-Version: 2022-11-28" /repos/igorpecovnik/scripts/actions/workflows
        exit 1
        
  Stop:
    name: "Merge artifacts"
    if: failure()
    needs: build
    runs-on: ubuntu-latest
    steps:

      - name: "Runner clean"
        run: |
        
          ID=$(gh api "/repos/igorpecovnik/scripts/actions/workflows" | jq '.workflows[] | select(.name=="ABC")' | jq -r '.id')
          echo "ID=$ID"
